/**
 * The Engine interface defines a contract for all game engine components.
 * Each engine must implement the `update` method to handle periodic updates.
 */

public interface Engine {
    void update();
}